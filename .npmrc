# pnpm configuration for Nuxt 3 monorepo
# Required for proper hoisting of Nuxt virtual modules

# Use shamefully-hoist to ensure Nuxt can access all dependencies
shamefully-hoist=true

# Hoist all dependencies to root node_modules
hoist-pattern[]=*

# Strict peer dependencies off to avoid conflicts
strict-peer-dependencies=false
