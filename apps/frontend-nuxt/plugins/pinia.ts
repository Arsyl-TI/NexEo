// Pinia is registered by @pinia/nuxt.
// Keep this plugin as the domain integration boundary for future persistence setup.
export default defineNuxtPlugin(() => {
  // Intentionally empty during the scaffold phase.
})
